<!doctype html>
<html>
<head>
	<% include ./head %>
</head>
<% include ./header %>


	<div class="page-header text-center">
		<% if (isShowOwner) { %>
		<button class="btn pull-right btn-success" onclick="location.href='/auditions/addaudition';"> Create Audition</button>
		<% } %>
		<h2>Auditions</h2>
	</div>

	<div class="container">
		<%=auditions%>
	</div>

        </div>
        <!-- /#page-content-wrapper -->

    </div>
    <!-- /#wrapper -->
</body>
</html>